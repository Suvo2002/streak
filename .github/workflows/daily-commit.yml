name: Daily Streak Commit

on:
  schedule:
    # ── Primary Run: 10:00 PM IST (16:30 UTC) ──────────────────────────────────
    - cron: "30 16 * * *"
    # ── Fallback 1: 11:00 PM IST (17:30 UTC) — 1 hour after primary ───────────
    - cron: "30 17 * * *"
    # ── Fallback 2: 11:55 PM IST (18:25 UTC) — end-of-day safety backup ────────
    - cron: "25 18 * * *"
    # ── Morning Backup: 10:00 AM IST (04:30 UTC) ──────────────────────────────
    - cron: "30 4 * * *"

  workflow_dispatch:
    inputs:
      force:
        description: "Force commit even if already committed today"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      dry_run:
        description: "Run without pushing (for testing)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

# ── Least-privilege permissions ──────────────────────────────────────────────
permissions:
  contents: write   # push commits
  issues: write     # create failure-notification issues

# ── Prevent overlapping runs ─────────────────────────────────────────────────
concurrency:
  group: daily-streak
  cancel-in-progress: false

env:
  BOT_NAME: "Suvo2002"
  BOT_EMAIL: "mail2suvodas@gmail.com"
  TARGET_BRANCH: "main"                # change to a dedicated branch if preferred
  STATUS_FILE: "log/status.json"       # audit log of every run

jobs:
  streak:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_BRANCH }}
          fetch-depth: 0               # full history for day-count calculation

      # ── 2. Set up Python ───────────────────────────────────────────────────
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # ── 3. Generate today's content ────────────────────────────────────────
      - name: Generate daily content
        id: generate
        env:
          FORCE_COMMIT: ${{ github.event.inputs.force || 'false' }}
        run: |
          python scripts/generate_content.py

      # ── 4. Append audit status log ────────────────────────────────────────
      - name: Update audit log
        if: steps.generate.outputs.already_committed != 'true'
        run: |
          mkdir -p "$(dirname "$STATUS_FILE")"
          TIMESTAMP=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "{\"timestamp\":\"${TIMESTAMP}\",\"status\":\"pushed\",\"run_url\":\"${RUN_URL}\",\"trigger\":\"${{ github.event_name }}\"}" \
            >> "$STATUS_FILE"

      # ── 5. Commit & push ──────────────────────────────────────────────────
      - name: Commit and push
        id: commit
        if: steps.generate.outputs.already_committed != 'true'
        env:
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: |
          git config user.name  "$BOT_NAME"
          git config user.email "$BOT_EMAIL"

          # Stage all generated / modified files
          git add -A

          # Check if there are actual changes to commit
          if git diff --cached --quiet; then
            echo "status=skipped" >> "$GITHUB_OUTPUT"
            echo "ℹ️  No changes detected — skipping commit."
            exit 0
          fi

          DATE=$(date -u +%Y-%m-%d)
          git commit -m "📅 daily log: ${DATE} — auto-generated streak entry"

          if [ "$DRY_RUN" = "true" ]; then
            echo "status=dry-run" >> "$GITHUB_OUTPUT"
            echo "🧪 Dry-run mode — commit created but not pushed."
          else
            git pull --rebase origin "$TARGET_BRANCH"
            git push origin "$TARGET_BRANCH"
            echo "status=pushed" >> "$GITHUB_OUTPUT"
            echo "✅ Commit pushed to ${TARGET_BRANCH}."
          fi

      # ── 6. Send Telegram notification ──────────────────────────────────────
      - name: Send Telegram notification
        if: steps.commit.outputs.status == 'pushed'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python scripts/notify_telegram.py

      # ── 7. Failure notification — auto-create a GitHub Issue ───────────────
      - name: Create failure issue
        if: failure()
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `🚨 Daily streak workflow failed — ${new Date().toISOString().slice(0, 10)}`,
              body: [
                `The **Daily Streak Commit** workflow failed.`,
                ``,
                `| Detail | Value |`,
                `|--------|-------|`,
                `| **Run** | [View logs](${runUrl}) |`,
                `| **Trigger** | \`${context.eventName}\` |`,
                `| **Branch** | \`${process.env.TARGET_BRANCH}\` |`,
                `| **Time (UTC)** | ${new Date().toISOString()} |`,
                ``,
                `Please investigate and re-run manually if the streak is at risk.`,
              ].join('\n'),
              labels: ['streak-bot', 'bug'],
            });
